# Advocate — Final Position
Homepage = centered search bar only. Post-search facets (not pre-search pills).
Result cards: scripture ref → title → speaker/series/date. Flag icon for reports.
Admin = single form (4 fields + save). Mobile: sticky search, 44px targets, single column.
Non-negotiable: visible result count on every search.
