# Skeptic — Final Position
Owning the index = owning the attack surface. Index poisoning is the new critical risk.
Non-negotiable: sanitize crawled content to plain text before indexing.
Typesense admin key server-side only. Search via API route (no client key).
Crawler: allowlist targets, network isolation, respect robots.txt, size/timeout limits.
Launch checklist: 7 items, priority ordered.
